NEXT_PUBLIC_API_URL=http://localhost:3000

# Solo del servidor de Next (se lee al arrancar, no al compilar): donde llama a la API
# para la vista previa de los enlaces de snapshots. Si falta, usa NEXT_PUBLIC_API_URL;
# hace falta cuando NEXT_PUBLIC_API_URL va vacia (dashboard y API bajo el mismo dominio).
# API_INTERNAL_URL=http://api:3000
